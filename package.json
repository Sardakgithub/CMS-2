{
  "name": "chronos-monorepo",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "turbo": "^2.1.1",
    "typescript": "^5.4.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  }
}
