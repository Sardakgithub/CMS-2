import LoginForm from '../../components/auth/LoginForm';

export const metadata = {
  title: 'Login - Chronos',
  description: 'Sign in to your Chronos account',
};

export default function LoginPage() {
  return <LoginForm />;
}
