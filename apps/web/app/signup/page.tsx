import SignupForm from '../../components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - Chronos',
  description: 'Create a new Chronos account',
};

export default function SignupPage() {
  return <SignupForm />;
}
